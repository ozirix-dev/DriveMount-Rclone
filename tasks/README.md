# Tasks

No real scheduled-task definitions are stored in V1.

Use this folder later if you want to add:

- task notes
- example task definitions
- task deployment helpers

The intended future operator task is to run `scripts\start-z-drive.ps1`.
