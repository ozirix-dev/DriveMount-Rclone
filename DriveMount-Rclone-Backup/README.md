# DriveMount-Rclone-Backup

This directory is the Proton-backed local backup root for the `DriveMount-Rclone` project.

- for snapshots, exports, and other recovery artifacts
- Git tracks only this `README.md`
- backup payload stays out of git
